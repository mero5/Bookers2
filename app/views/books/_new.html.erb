<%#左下book/新規作成%>
<h2>New Book</h2>
<%= form_with model: book do |f| %>
  <label>title</label>
    <%= f.text_field :title %>
  <label>opinion</label>
    <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>